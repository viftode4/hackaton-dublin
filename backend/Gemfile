source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby ">= 3.2"

# Rails
gem "rails", "~> 7.1.0"
gem "puma", "~> 6.0"

# Database
# gem "pg", "~> 1.5"  # Use SQLite for dev to avoid native extension issues
gem "sqlite3"

# API
gem "rack-cors"

# External APIs
gem "anthropic", "~> 1.23"  # Official Anthropic SDK with tool_runner
gem "stripe"
gem "paid_ruby"             # Paid.ai — AI agent usage metering, billing, cost tracking

# Timezone data (required for Alpine Linux)
gem "tzinfo-data"

# Environment
gem "dotenv-rails", groups: [:development, :test]

# Serialization
gem "active_model_serializers"

# Logging (optional but useful)
gem "rails_semantic_logger"

# Development & Testing
group :development, :test do
  # gem "debug", platforms: %i[mri mingw x64_mingw]  # Skip debug for Windows
  gem "pry-rails"
end

group :development do
  # gem "web-console"  # Skip heavy dev tools
end
